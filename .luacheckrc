std = "lua54"

-- Show warning codes so it is easier to suppress specific issues later.
codes = true

max_line_length = 120

globals = {
   -- Add intentional app-level globals here, but prefer locals/modules.
}
